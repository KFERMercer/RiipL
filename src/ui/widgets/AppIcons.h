#pragma once

#include <QIcon>

class AppIcons
{
public:
    static QIcon swapHorizontal();
    static QIcon moveUp();
    static QIcon moveDown();
    static QIcon reset();
    static QIcon undo();
    static QIcon redo();
};
