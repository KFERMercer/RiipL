# RiipL - AI 驱动的桌面翻译应用

RiipL 是一款基于 Qt 构建的跨平台桌面翻译应用，旨在复制 DeepL 的功能开发一个功能强大的翻译软件。它支持多种自定义选项，包括提示词工程、术语表、多种语气和风格，以及文档翻译功能。

## 项目概述

### 构建步骤
```bash
cmake --build build -j$(nproc) --clean-first 2>&1
```

### 运行
```bash
timeout 5 ./build/RiipL 2>&1; echo "exit: $?"
```

### 代码风格
- 遵循 Qt 编码风格。
- 尽量使用 Qt 标准库。
- 使用 Qt 原生控件。

### 终端命令规则
- 使用相对路径操作项目根目录下的文件。
- 严禁 读取/写入/查找 项目目录外的文件。
